# How to calculate your pay using Python 3.6

hours = float(input('Enter the number of hours worked: '))
wage = float(input('Enter your wage per hour: '))
pay = float(hours) * float(wage)

print(pay)
